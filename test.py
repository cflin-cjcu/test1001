import cv2

print("AAA")

cv2.imread()

# Import only if not previously imported
VariableName = cv2.imread("Address", flag)

# Import only if not previously imported
contours, hierarchy = cv2.findContours(binaryImg, cv2.retrievalMode, cv2.approximation method)
# Colors of contours is kept green by default
cv2.drawContours(Image, contours, contourIdx, (0, 255, 0), thickness)
# contourIdx = -1 if you want to draw all contours
